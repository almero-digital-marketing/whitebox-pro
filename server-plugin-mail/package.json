{
  "name": "whitebox-pro-server-plugin-mail",
  "version": "0.1.0",
  "description": "Mail channel for whitebox — outbound (transactional + bulk), inbound (form + provider webhook), tracking, suppressions, invalid list. Pluggable provider (whitebox-pro-mail-mailgun, whitebox-pro-mail-postmark, …).",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "whitebox-pro-server": "^2.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "libphonenumber-js": "^1.11.0",
    "multer": "^2.1.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  },
  "files": [
    "src",
    "README.md"
  ]
}
