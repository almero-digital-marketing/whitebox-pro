# Copy to .env.local. Must match the server's WB_ANALYTICS_TOKEN so the SPA can
# call the auth-gated /analytics endpoints. Leave blank only if the server runs
# the analytics plugin without an auth secret (dev).
VITE_ANALYTICS_TOKEN=
