{
  "name": "whitebox-pro-server-plugin-live",
  "version": "0.2.0",
  "description": "WhiteBox server — live plugin. A realtime operations view of everything moving through WhiteBox: what arrives (web, replies, CRM, conversions), what leaves (mail, SMS, webhooks), and delivery health. Owns no tables; reads the core event registry and taps notify()'s firehose channel.",
  "type": "module",
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "whitebox-pro-server": "^2.2.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
