{
  "name": "whitebox-pro-client-plugin-shortener",
  "version": "0.1.0",
  "description": "Whitebox browser client — shortener plugin. Reads a claim token from the landing URL, redeems it for the customer it was bound to, scrubs the token, and adopts that passport for the session.",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js"
  },
  "files": [
    "src",
    "README.md"
  ],
  "sideEffects": false,
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "whitebox-pro-client": "^0.2.0"
  },
  "devDependencies": {
    "happy-dom": "^15.0.0",
    "vitest": "^2.1.0"
  }
}
